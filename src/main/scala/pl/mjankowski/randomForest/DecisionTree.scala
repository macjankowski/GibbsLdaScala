package pl.mjankowski.randomForest

import pl.mjankowski.common.DataWithLabels

/**
  *
  * @author Maciej Jankowski <maciej.jankowski@wat.edu.pl>
  */
class DecisionTree {

//  def train(dataWithLabels: DataWithLabels): DecisionTreeModel = {
//
//  }

//  def findBestSplit(colWithLabel: Map[Double, Int], splitsNumber: Int): Double = {
//
//    val min = column.min
//    val max = column.max
//    val splits = Seq.range(min, max, (max - min)/splitsNumber.toDouble).toArray
//
//    val best: (Double, Double) = splits.map(split => (split, calculateGini(split))).toMap.minBy(_._1)
//    best._1
//  }

//  def calculateGini(split: Double, colWithLabel: Map[Double, Int]): Double = {
//
//    val classes = colWithLabel.filter{ case (d,l) => d < split}
//  }
}
